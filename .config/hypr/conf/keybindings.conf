# See https://wiki.hyprland.org/Configuring/Keywords/

# note to myself: if you "run out" of keybindings, do look at the Submap section at this link: https://wiki.hyprland.org/Configuring/Binds/   they allow you to enter in "modes", in different "modes", the same keybinding can have different effects (also you can enable a submap visualization in waybar)

$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# BINDINGS MADE BY ME (DECE)
bind = $mainMod, T, exec, $terminal        
bind = $mainMod, C, killactive,            
bind = $mainMod SHIFT, E, exit,                  # Move focus with mainMod + arrow keys
bind = $mainMod, F, exec, $fileManager     
bind = $mainMod, M, exec, $menu
bind = $mainMod SHIFT, F, togglefloating,        
bind = $mainMod, P, pseudo, # dwindle      
bind = $mainMod, L, togglesplit, # dwindle 

bind = $mainMod, B, exec, $browser

# window focusing
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
#bindm = $mainMod, mouse:273, resizewindow    # 273 is right mouse button
bindm = $mainMod SHIFT, mouse:272, resizewindow    # I changed to mainMod + SHIFT + left mouse to ease of use with trackpad

# Move windows with keyboard
bind = $mainMod SHIFT, left, swapwindow, l
bind = $mainMod SHIFT, right, swapwindow, r
bind = $mainMod SHIFT, up, swapwindow, u
bind = $mainMod SHIFT, down, swapwindow, d

# Resize windows with keyboard
$resAmt = 40
bind = $mainMod CTRL, left, resizeactive, -$resAmt 0
bind = $mainMod CTRL, right, resizeactive, $resAmt 0
bind = $mainMod CTRL, up, resizeactive, 0 -$resAmt
bind = $mainMod CTRL, down, resizeactive, 0 $resAmt

# Screen brightness
bind = , XF86MonBrightnessUp, exec, brightnessctl s +5%
bind = , XF86MonBrightnessDown, exec, brightnessctl s 5%-

# Volume and media control
bind = , XF86AudioRaiseVolume, exec, pamixer -i 5 
bind = , XF86AudioLowerVolume, exec, pamixer -d 5 
bind = , XF86AudioMicMute, exec, pamixer --default-source -m
bind = , XF86AudioMute, exec, pamixer -t
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous

# Screen Locking and Suspending
bind = $mainMod SHIFT, L, exec, hyprlock
bind = $mainMod CTRL, L, exec, wlogout

# Laptop Lid closed
#bindl = ,switch:on:Lid Switch,exec,hyprlock 

# Waybar
bind = $mainMod CTRL, B, exec, pkill -SIGUSR1 waybar
bind = $mainMod SHIFT, B, exec, ~/.config/waybar/scripts/launch.sh    # restart waybar

# Using Rofi to emulate window-tabs
bind = $mainMod, tab, exec, rofi -show window

# Screenshots
bind = , PRINT, exec, hyprshot -m region # set HYPRSHOT_DIR env variable to ~/Pictures/Screenshots/ in hyprland.conf file

# Clipboard manager
bind = $mainMod, v, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy
